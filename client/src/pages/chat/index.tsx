import styles from './index.module.less';
import Container from '../container';

const Chat = () => {
	return (
		<>
			<div className={styles.bgContainer}>
				<Container />
			</div>
		</>
	);
};

export default Chat;
